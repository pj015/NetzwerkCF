---
title: "Pretest Christiane F. WS2020"
output: html_notebook
---

```{r Pretest Buch 2 - Kapitel 1} 

# Codebuch für den Datensatz:

library("igraph")
library(igraphdata)

# Einlesen der Edge- und Nodelist
# Datensatz unter: https://github.com/pj015/NetzwerkCF

edges <- read.csv("Edge-List.csv", header=T, as.is=T, sep = ",") 
nodes <- read.csv("Node-List.csv",header=T, as.is=T, sep = "," ) 

# erstellt ein igraph-objekt aus der Fusion von Edge- und Nodelist
edgematrix <-as.matrix(edges) 

c <- graph_from_data_frame(d=edgematrix, vertices=nodes, directed = FALSE)
# das igraph-Objekt heisst jetzt "c" (für das Netzwerk Christiane F. ) und ist ein gerichtetes Netzwerk.

# Einfache Visualisierung

plot(c)
   getwd() 

   
```
